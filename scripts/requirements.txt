# 灯台 収集・要約・台本・音声パイプライン
# 収集/要約/台本は標準ライブラリのみで動く（RSS/HTTPは urllib、要約フォールバックも自前）。
# 音声のみ外部ライブラリを使う。すべて無料・APIキー登録不要。
edge-tts>=6.1.0     # 無料の音声合成（Microsoft Edge の読み上げ）
mutagen>=1.47.0     # mp3の長さ検証（5分超過チェック）用
